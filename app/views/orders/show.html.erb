

<div>
  <h3> Your order </h3>
    <% @your_order.products.each do |prod| %>
    <h4> Ice cream flavor</h4>
    <%= prod.name %>
    <h4> price (10 gallons) </h4>
    <%= prod.price %>
   <% end %>
</div>
